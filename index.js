let startNum = 8; // Change this number to test with different values

// TODO: Use a while loop for the reverse countdown

while (startNum >= 0) {
    console.log(startNum);
    startNum --; //This line decrements startNum by 1 in each iteration.
}